#4.0 Setup

1. React RouterDom 설치: npm i react-router-dom react-query

Nested(중첩된) Router란, 한 스크린 내에 또 다른 Router를 가질 수 있음을 의미

2. src안에 routes 폴더 만들기 (coin.tsx, coins.tsx)

3. 바깥에 Router.tsx 폴더 생성

4. npm i --save-dev @types/react-router-dom //6버전으로 진행시 설치하지 않아도 타입을 불러옴
