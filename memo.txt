#5.9 Custom Validation

validate는 함수를 값으로 가질 건데, 이 함수는 인자로 항목에 현재 쓰여지고 있는 값을 받을 것이다. => true 나 false 반환

validate: (value) => false

==> 항상 false를 반환, 에러에 type:"validate" 가 뜰 것이다.

validate: (value) => !value.includes("kang") 
kang이 아니면 true 반환

validate: (value) => "hello"
문자열이면 false 반환
